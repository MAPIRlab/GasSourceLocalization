########################################################################
# Local parameters for the COSTMAP_2D pkg (navigation stack)
########################################################################
local_costmap:
  plugins:
    - {name: static_map, type: "costmap_2d::StaticLayer" }
    - {name: obstacles, type: "costmap_2d::VoxelLayer"}
    - {name: inflation, type: "costmap_2d::InflationLayer" }
#   - {name: proxemic, type: "social_navigation_layers::ProxemicLayer" }
    
  global_frame: map
  robot_base_frame: base_link
  update_frequency: 10.0
  publish_frequency: 10.0
  rolling_window: true
  width: 6.0
  height: 6.0
  resolution: 0.05
  min_obstacle_height: -10
  always_send_full_costmap: true
    
  obstacles:
    track_unknown_space: true
  
  inflation_layer:
    inflation_radius: 0.3          # how far away the zero cost point is from obstacle. Must be > robot_radius
    cost_scaling_factor: 5.0        # decay curve. Setting it higher will make the decay curve more steep
  
 
  #proxemic:
  # amplitude: 150.0
  # covariance: 0.1
  # cutoff: 20.0
  # factor: 1.0
